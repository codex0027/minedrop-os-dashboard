# Secrets — never commit
.env
.env.*
*.token
*.secret
credentials.json
config.local.*

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*~

# Node (for future backend)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Logs
*.log
logs/

# Build outputs (for future)
dist/
build/
.cache/

# Backups (the real ones, not the demo list)
backups/
*.bak
*.dump
